language: ruby
cache: bundler

rvm:
  - jruby
  - 2.1.0
  - 2.0.0

script: 'bundle exec rake'

notifications:
  email:
    recipients:
      - ori@pekelman.com
    on_failure: change
    on_success: never